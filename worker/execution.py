from typing import Tuple, List


def estimate_edge(e, state) -> Tuple[float, List[str], float]:
    """
    Returns:
      edge_bps: expected net basis points
      reasons: human-readable list of strings
      size_cap_usd: maximum safe trade size in USD
    """
    # Phase 1 placeholder: execution logic disabled
    edge_bps = 0.0
    reasons: List[str] = []
    size_cap_usd = 0.0

    # stub for future execution logic
    return edge_bps, reasons, size_cap_usd
